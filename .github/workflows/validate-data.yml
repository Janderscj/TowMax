name: Validate data

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check-setup:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Say hello
        run: echo "GitHub Actions is wired up."
